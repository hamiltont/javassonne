#summary Describes IntPair
#labels Phase-Implementation

= Introduction =

IntPair is based on a gnu class that's based on LISP's cons cell. It is restricted to dotted pairs of integers


= Details =

car() returns the first integer in the pair.
cdr() returns the second integer in the pair.